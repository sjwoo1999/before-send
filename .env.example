# Before Send (보내기 전에) - Environment Variables

# =====================================================
# Mock Mode (for testing without external dependencies)
# =====================================================

# Set to 'true' to run without Supabase/Claude API
NEXT_PUBLIC_MOCK_MODE=false

# =====================================================
# Supabase Configuration
# =====================================================

# Public URL for Supabase project
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Public anonymous key (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key (server-side only, never expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =====================================================
# AI Configuration
# =====================================================

# Anthropic Claude API key
CLAUDE_API_KEY=sk-ant-api03-...

# =====================================================
# Rate Limiting (Optional)
# =====================================================

# Upstash Redis URL for rate limiting
# If not set, uses in-memory rate limiting (resets on server restart)
RATE_LIMIT_REDIS_URL=https://your-redis.upstash.io
RATE_LIMIT_REDIS_TOKEN=your-redis-token

# =====================================================
# Deployment
# =====================================================

# Base URL of the application (for absolute URLs in emails, etc.)
NEXT_PUBLIC_BASE_URL=https://before-send.vercel.app
